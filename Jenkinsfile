pipeline{
    agent {
        dockerfile true
    }
    // agent any
    // tools {dockerTool  "docker" } 
    stages {
        stage ('Test') {
            steps {
                echo "HELLO WORLD"
            }
        }
    }
}